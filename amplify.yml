version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
        - npm run prebuild
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: dist/contatos-app/browser
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*      
